import { theme as chakraTheme } from '@chakra-ui/core';

export const theme = Object.assign(chakraTheme, {
  fonts: {
    body: '"Baskerville", sans-serif',
    heading: '"Baskerville", sans-serif',
    mono: 'Menlo, monospace',
  },
});